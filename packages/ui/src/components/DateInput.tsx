import { DateInput as MantineDateInput } from '@mantine/dates';

export type { DateInputProps } from '@mantine/dates';

/** Clementine DateInput — free-text date field with parsing + calendar popover. Spec: specs/date-input/index.md */
export const DateInput: typeof MantineDateInput = MantineDateInput;
