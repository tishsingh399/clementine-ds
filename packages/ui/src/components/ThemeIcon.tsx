import { ThemeIcon as MantineThemeIcon } from '@mantine/core';

export type { ThemeIconProps } from '@mantine/core';

/** Clementine ThemeIcon — a filled container that gives an icon a consistent shape + background. Decorative; label the meaning elsewhere. Spec: specs/theme-icon/index.md */
export const ThemeIcon: typeof MantineThemeIcon = MantineThemeIcon;
