import { NumberInput as MantineNumberInput } from '@mantine/core';

export type { NumberInputProps } from '@mantine/core';

/** Clementine NumberInput — A numeric input with optional steppers, min/max, and formatting (seats, quotas, counts). Spec: specs/number-input/index.md */
export const NumberInput: typeof MantineNumberInput = MantineNumberInput;
