import { Button as MantineButton } from '@mantine/core';

export type { ButtonGroupProps } from '@mantine/core';

/** Clementine ButtonGroup — a set of related buttons joined into one control. Spec: specs/button-group/index.md */
export const ButtonGroup: typeof MantineButton.Group = MantineButton.Group;
