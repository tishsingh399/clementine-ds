import { Carousel as MantineCarousel } from '@mantine/carousel';

export type { CarouselProps } from '@mantine/carousel';

/** Clementine Carousel — horizontally scrollable slides with controls + indicators (embla-backed). Spec: specs/carousel/index.md */
export const Carousel: typeof MantineCarousel = MantineCarousel;
