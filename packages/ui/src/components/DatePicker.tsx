import { DatePicker as MantineDatePicker } from '@mantine/dates';

export type { DatePickerProps } from '@mantine/dates';

/** Clementine DatePicker — inline calendar for choosing a single date, range, or multiple dates. Spec: specs/date-picker/index.md */
export const DatePicker: typeof MantineDatePicker = MantineDatePicker;
