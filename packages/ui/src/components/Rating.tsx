import { Rating as MantineRating } from '@mantine/core';

export type { RatingProps } from '@mantine/core';

/** Clementine Rating — Capture or display a star rating (feedback score, quality). Spec: specs/rating/index.md */
export const Rating: typeof MantineRating = MantineRating;
