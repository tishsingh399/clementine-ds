import { FileInput as MantineFileInput } from '@mantine/core';

export type { FileInputProps } from '@mantine/core';

/** Clementine FileInput — A control to pick one or more files, showing the chosen file name(s). Spec: specs/file-input/index.md */
export const FileInput: typeof MantineFileInput = MantineFileInput;
