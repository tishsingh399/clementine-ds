import { Anchor as MantineAnchor } from '@mantine/core';

export type { AnchorProps } from '@mantine/core';

/** Clementine Anchor — a text link. Uses text.link; underline on hover/focus, never color alone. Spec: specs/anchor/index.md */
export const Anchor: typeof MantineAnchor = MantineAnchor;
