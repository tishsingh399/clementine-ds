import { Indicator as MantineIndicator } from '@mantine/core';

export type { IndicatorProps } from '@mantine/core';

/** Clementine Indicator — a notification dot/count overlaid on an element (avatar, icon, tab). Spec: specs/indicator/index.md */
export const Indicator: typeof MantineIndicator = MantineIndicator;
