import { Divider as MantineDivider } from '@mantine/core';

export type { DividerProps } from '@mantine/core';

/** Clementine Divider — a horizontal or vertical rule, optionally labelled. Spec: specs/divider/index.md */
export const Divider: typeof MantineDivider = MantineDivider;
