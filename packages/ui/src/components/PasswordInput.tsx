import { PasswordInput as MantinePasswordInput } from '@mantine/core';

export type { PasswordInputProps } from '@mantine/core';

/** Clementine PasswordInput — A masked text input with a visibility toggle, for passwords and secrets. Spec: specs/password-input/index.md */
export const PasswordInput: typeof MantinePasswordInput = MantinePasswordInput;
