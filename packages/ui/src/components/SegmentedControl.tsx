import { SegmentedControl as MantineSegmentedControl } from '@mantine/core';

export type { SegmentedControlProps } from '@mantine/core';

/** Clementine SegmentedControl — pick one of a small set of mutually-exclusive options (a styled radio group). Spec: specs/segmented-control/index.md */
export const SegmentedControl: typeof MantineSegmentedControl = MantineSegmentedControl;
