import { Slider as MantineSlider } from '@mantine/core';

export type { SliderProps } from '@mantine/core';

/** Clementine Slider — Select a value from a continuous range by dragging (thresholds, volume, sensitivity). Spec: specs/slider/index.md */
export const Slider: typeof MantineSlider = MantineSlider;
