import { Breadcrumbs as MantineBreadcrumbs } from '@mantine/core';

export type { BreadcrumbsProps } from '@mantine/core';

/** Clementine Breadcrumbs — hierarchical trail to the current page. Renders a `nav`; mark the last item current. Spec: specs/breadcrumbs/index.md */
export const Breadcrumbs: typeof MantineBreadcrumbs = MantineBreadcrumbs;
