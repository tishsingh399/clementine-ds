import { MultiSelect as MantineMultiSelect } from '@mantine/core';

export type { MultiSelectProps } from '@mantine/core';

/** Clementine MultiSelect — Pick several values from a list; selections show as removable pills. Spec: specs/multi-select/index.md */
export const MultiSelect: typeof MantineMultiSelect = MantineMultiSelect;
