import { Kbd as MantineKbd } from '@mantine/core';

export type { KbdProps } from '@mantine/core';

/** Clementine Kbd — renders a keyboard key/shortcut (⌘, Ctrl, Enter). Spec: specs/kbd/index.md */
export const Kbd: typeof MantineKbd = MantineKbd;
