node_modules/
dist/
storybook-static/
.turbo/
.DS_Store
*.log

# Secrets — NEVER commit. Use .env.example as the safe template.
.env
.env.local
.env.*.local

# Local tooling artifacts (not part of the system)
.playwright-mcp/
*.fig

# Generated reports — produced by scripts/, regenerated in CI (don't commit stale snapshots)
apps/observatory/parity-report.json
apps/observatory/figma-expected-variables.json
